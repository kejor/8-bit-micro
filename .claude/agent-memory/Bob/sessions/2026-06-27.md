# Session — 2026-06-27

## Topics Covered
- Automated testing for full-custom analog/mixed-signal designs
- Decomposing "automated testing" into two separable problems: (1) running the sim headlessly, (2) machine-judging PASS/FAIL
- Current state of Kevin's loop: GUI Netlist+Run, then manual eyeball inspection of `plot v(A) v(B) v(C)`
- Pointed him toward `.MEASURE` as the mechanism to turn a mental pass criterion into a checkable scalar
- Seeded the idea of ngspice batch mode (no GUI) + a bash script iterating over all testbenches in the Docker container

## Where We Left Off
Kevin answered the first anchor: "there're multiple criterion, the most important is that the logic of the AND is correct." Good — he named logical correctness as primary. I then pushed him to define what "logic correct" means in the SPICE/analog world, asking two follow-ups:
1. What voltage must the simulator actually see to count as a legitimate HIGH (and LOW)? Where exactly is the line on a 3.3 V supply — is 2.9 V high? 2.0 V?
2. How many distinct input combinations of the AND truth table must he drive before claiming correctness (i.e. all 4 rows, not just the easy one)?
Still withholding the script/`.MEASURE` syntax until he commits to concrete voltage thresholds and the all-rows requirement.

## Kevin's Current Understanding
- Has working testbenches (inverter, XOR, half-adder, AND) with MODELS + NGSPICE code_shown blocks, `.PARAM`, `.control` with TRAN/write/plot.
- Currently relies entirely on visual plot inspection — no `.MEASURE`, no scripted pass/fail yet.
- Thinks of correctness in Boolean terms ("the logic is correct") but has not yet translated that into voltage thresholds or an explicit all-rows truth-table check. This is the gap I'm working on.
- Comfortable launching xschem from the container CLI; container has project mounted at /foss/designs.

## Next Session Hook
Get Kevin to commit to concrete numbers: a HIGH threshold (e.g. > 0.9*VDD) and LOW threshold (e.g. < 0.1*VDD), and the requirement to exercise all 4 truth-table rows. Then guide: (a) add a `.MEASURE` capturing V(out) at a settled time per input combo, (b) compare against the threshold to emit a scalar, (c) discover `ngspice -b` batch mode, (d) reason out a bash loop over every `*_tb` netlist grepping logs for PASS/FAIL. Resume at: "What voltage counts as HIGH, and how do you get .MEASURE to sample V(out) after the output has settled for each of the 4 input rows?"

## Project Context (for resuming)
- Original 8-bit MCU, full-custom, GF180MCU-D PDK
- Tooling: Xschem + ngspice -> Magic -> Netgen (DRC/LVS)
- Deadline: 2026-10-01; educational goal is primary
- Completed cells: inverter, XOR, half-adder (schematic + symbol + testbench)
